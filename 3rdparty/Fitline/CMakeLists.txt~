# make sure we find all headers
include_directories(${EdgeBasedTracker_SOURCE_DIR}/)

find_package(OpenCV)

# we create a library for line fitting
add_library(LineFitLib fitline.cpp LFLineFitter.cpp LFLineSegment.cpp)

#add_executable(Fitline fitline.cpp)
#target_link_libraries(Fitline ${OpenCV_LIBS} ${EdgeBasedTracker_SOURCE_DIR}/Fitline/libLineFitLib.a ${OpenCV_LIBS} ${EdgeBasedTracker_SOURCE_DIR}/Image/libImageLib.a)